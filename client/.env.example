# Public HTTPS URL of the Express + Socket.IO server (no trailing slash).
# Required for Vercel/production. Local dev defaults to http://localhost:3030.
REACT_APP_API_URL=https://your-api.example.com
