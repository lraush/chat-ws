{
  "name": "chat-ws",
  "private": true,
  "scripts": {
    "test": "npm test --prefix server && CI=true npm test --prefix client -- --watchAll=false",
    "test:server": "npm test --prefix server",
    "test:client": "CI=true npm test --prefix client -- --watchAll=false"
  }
}
